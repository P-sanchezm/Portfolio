# Profile

Add your profile photo here as `pablo-photo.png` (square works best — it's
shown in the hero glass card). Until then, a gradient avatar with your initials
is displayed automatically.

Referenced from `src/data/profile.ts` via the `photo` field.
